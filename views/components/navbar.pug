nav
    .nav-left
        img(src="/static/icons/android-chrome-192x192.png" alt="logo")
    .nav-center 
        input#searchbar(type="text", name="search", placeholder="Znajdź produkt..." onkeyup="search_product()")
    .nav-right
        a(href="/", class="link")         Sklep
        if user
            if role === 'admin'
                a(href="/admin").link Panel Admina
            a(href="/api/logout").link  Wyloguj się
        else
            a(href="/login").link   Zaloguj się
        a(href="/cart").link Koszyk
